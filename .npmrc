# @vercel/analytics declares an optional @sveltejs/kit peer that otherwise
# drags in a conflicting Vite major. We don't use Svelte; relax peer resolution
# so installs are reproducible locally and on Vercel.
legacy-peer-deps=true
